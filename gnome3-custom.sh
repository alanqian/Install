# dconf-editor, It's part of the package dconf-tools

gsettings set com.canonical.indicator.datetime custom-time-format "'%a, %d. %h %H:%M'"

# dconf-editor (from package dconf-tools) and go to org.gnome.desktop.wm.keybindings.

